# Template file parser

Ce dossier contient la copie locale du parser `template-file` auparavant consommé via `@fabnum/prelevements-deau-timeseries-parsers@0.0.11`.

Le code a été déplacé dans le front car c'est le seul consommateur restant et parce qu'aucune nouvelle publication npm n'est prévue. Éviter les modifications fonctionnelles ici sans test de non-régression sur les fixtures.
